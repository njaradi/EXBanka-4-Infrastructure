apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-service
  namespace: exbanka-4
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client-service
  template:
    metadata:
      labels:
        app: client-service
    spec:
      containers:
        - name: client-service
          image: ghcr.io/raf-si-2025/exbanka-4-backend/client-service:latest
          imagePullPolicy: Always
          env:
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: exbanka-db-pguser-client-user
                  key: uri
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            tcpSocket:
              port: 50056
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: exbanka-4
spec:
  selector:
    app: client-service
  ports:
    - port: 50056
      targetPort: 50056
