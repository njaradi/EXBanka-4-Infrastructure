apiVersion: apps/v1
kind: Deployment
metadata:
  name: exchange-service
  namespace: exbanka
spec:
  replicas: 1
  selector:
    matchLabels:
      app: exchange-service
  template:
    metadata:
      labels:
        app: exchange-service
    spec:
      containers:
        - name: exchange-service
          image: ghcr.io/raf-si-2025/exbanka-4-backend/exchange-service:latest
          imagePullPolicy: Always
          env:
            - name: EXCHANGE_DB_URL
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: exchange-db-url
            - name: ACCOUNT_DB_URL
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: account-db-url
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            tcpSocket:
              port: 50057
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: exchange-service
  namespace: exbanka
spec:
  selector:
    app: exchange-service
  ports:
    - port: 50057
      targetPort: 50057
